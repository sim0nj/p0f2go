# 性能（Performance）

## 评估方法
- 指标：事件速率、CPU、内存、丢弃数
- 工具：压测与 pcap 重放

## 模式对比
- RAW：部署简单，性能中等，容器环境友好
- XDP：高性能低开销，需宿主支持
- TC（规划）：兼容性强，性能介于 RAW 与 XDP

## 控制手段
- sample：降低事件量，保留代表性
- rate：限制输出峰值，保护下游

## 建议
- 中等 QPS 采用 RAW + 采样/限速
- 高 QPS 优先 XDP；RAW 作为后备
